name: Remote Server Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy FastAPI App locally
        run: |
          cd /var/www/MMIXM_demo
          git pull origin main
          sudo systemctl daemon-reload
          sudo systemctl restart fastapi
